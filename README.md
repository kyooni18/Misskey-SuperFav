# Misskey-SuperFav

Vanila Misskey, just flavored with 'Super Favorite'. (idk i didnt even localized this one)

#Installation

at cloned folder:

```zsh
docker compose -f compose.superfavorite.yml up -d --build
(additional settings are required for actual setup)
```
